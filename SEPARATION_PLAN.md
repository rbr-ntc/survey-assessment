# План разделения Frontend и Backend

## 🎯 Цель
Разделить монолитное приложение на два независимых сервиса:
- **Frontend (Next.js)** → Деплой на **Vercel**
- **Backend (FastAPI)** → Деплой на **Railway**

---

## 📋 Переменные окружения

### 🔵 Frontend (Vercel)

#### Обязательные переменные:
```bash
NEXT_PUBLIC_API_URL=https://your-backend.railway.app
NEXT_PUBLIC_API_KEY=your-secret-api-key-here
NEXT_PUBLIC_ENABLE_QUICK_TEST=true  # или false
```

#### Описание:
- `NEXT_PUBLIC_API_URL` - URL вашего бэкенда на Railway (например: `https://api.yourdomain.com` или Railway URL)
- `NEXT_PUBLIC_API_KEY` - API ключ для аутентификации запросов к бэкенду (должен совпадать с `API_KEY` на бэкенде)
- `NEXT_PUBLIC_ENABLE_QUICK_TEST` - флаг включения быстрого теста (`true`/`false`)

**Важно:** В Next.js переменные, начинающиеся с `NEXT_PUBLIC_`, доступны в браузере. Не храните секретные данные в таких переменных!

---

### 🔴 Backend (Railway)

#### Обязательные переменные:
```bash
# API Configuration
API_KEY=your-secret-api-key-here  # Должен совпадать с NEXT_PUBLIC_API_KEY на фронтенде
SECRET_KEY=your-very-secret-key-for-jwt-tokens

# OpenAI
OPENAI_API_KEY=sk-proj-...
OPENAI_MODEL=gpt-5.2-mini
OPENAI_MAX_TOKENS=4000
OPENAI_REASONING_EFFORT=medium  # none, low, medium, high

# Database
MONGO_URL=mongodb://username:password@host:port/database?authSource=admin

# CORS (критично для интеграции!)
CORS_ORIGINS=https://your-frontend.vercel.app,https://yourdomain.com

# Features
ENABLE_QUICK_TEST=true  # или false

# Optional
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000
LOG_LEVEL=INFO
ACCESS_TOKEN_EXPIRE_MINUTES=30
```

#### Описание:
- `API_KEY` - ключ для аутентификации API запросов (должен совпадать с `NEXT_PUBLIC_API_KEY`)
- `SECRET_KEY` - секретный ключ для JWT токенов (используйте длинную случайную строку)
- `OPENAI_API_KEY` - ваш ключ OpenAI API
- `MONGO_URL` - строка подключения к MongoDB (можно использовать Railway MongoDB или внешний)
- `CORS_ORIGINS` - список разрешенных доменов фронтенда через запятую (критично для работы!)
- `CORS_ORIGINS` должен включать:
  - URL вашего фронтенда на Vercel (например: `https://your-app.vercel.app`)
  - Ваш кастомный домен (если есть, например: `https://yourdomain.com`)

---

## 🔗 Интеграция между Frontend и Backend

### 1. CORS настройка (Backend)

Backend должен разрешать запросы с домена фронтенда. В `backend/app/config.py` уже есть валидация:

```python
CORS_ORIGINS: str = os.environ.get("CORS_ORIGINS", "http://localhost:3000")
```

**На Railway установите:**
```bash
CORS_ORIGINS=https://your-frontend.vercel.app,https://yourdomain.com
```

### 2. API URL на Frontend

Frontend должен знать URL бэкенда. В Vercel установите:
```bash
NEXT_PUBLIC_API_URL=https://your-backend.railway.app
```

Или если у вас кастомный домен для бэкенда:
```bash
NEXT_PUBLIC_API_URL=https://api.yourdomain.com
```

### 3. API Key синхронизация

`NEXT_PUBLIC_API_KEY` на фронтенде должен совпадать с `API_KEY` на бэкенде.

---

## 🌐 Домены и SSL

### Вариант 1: Использование доменов платформ (проще)

**Frontend (Vercel):**
- Автоматический домен: `your-app.vercel.app` (SSL автоматически)
- Кастомный домен: `yourdomain.com` (SSL через Vercel автоматически)

**Backend (Railway):**
- Автоматический домен: `your-backend.railway.app` (SSL автоматически)
- Кастомный домен: `api.yourdomain.com` (SSL через Railway автоматически)

### Вариант 2: Кастомные домены (рекомендуется для продакшена)

1. **Купите домен** (например, через Namecheap, GoDaddy, Cloudflare)
2. **Настройте DNS записи:**

   Для Frontend (Vercel):
   ```
   A record: @ → IP адрес Vercel (указан в настройках)
   CNAME: www → cname.vercel-dns.com
   ```

   Для Backend (Railway):
   ```
   CNAME: api → your-backend.railway.app
   ```

3. **SSL сертификаты:**
   - Vercel автоматически выдает SSL для кастомных доменов
   - Railway автоматически выдает SSL для кастомных доменов
   - Никаких self-signed сертификатов не нужно!

---

## 📦 Деплой на Vercel (Frontend)

### Шаги:

1. **Подключите репозиторий к Vercel:**
   - Зайдите на [vercel.com](https://vercel.com)
   - Import Git Repository
   - Выберите ваш репозиторий

2. **Настройте проект:**
   - **Root Directory:** `system-analyst-assessment`
   - **Framework Preset:** Next.js
   - **Build Command:** `npm run build` (по умолчанию)
   - **Output Directory:** `.next` (по умолчанию)

3. **Добавьте переменные окружения:**
   - Settings → Environment Variables
   - Добавьте все переменные из раздела "Frontend (Vercel)"

4. **Деплой:**
   - Vercel автоматически задеплоит при каждом push в main
   - Или можно деплоить вручную через Dashboard

### Структура проекта на Vercel:
```
Repository Root
└── system-analyst-assessment/  ← Root Directory в Vercel
    ├── src/
    ├── package.json
    └── ...
```

---

## 🚂 Деплой на Railway (Backend)

### Шаги:

1. **Создайте проект на Railway:**
   - Зайдите на [railway.app](https://railway.app)
   - New Project → Deploy from GitHub repo
   - Выберите ваш репозиторий

2. **Настройте сервис:**
   - **Root Directory:** `backend`
   - **Build Command:** (Railway определит автоматически из Dockerfile)
   - **Start Command:** (из Dockerfile)

3. **Добавьте MongoDB (опционально):**
   - Railway → New → Database → MongoDB
   - Скопируйте `MONGO_URL` из настроек MongoDB
   - Или используйте внешний MongoDB (MongoDB Atlas)

4. **Добавьте переменные окружения:**
   - Settings → Variables
   - Добавьте все переменные из раздела "Backend (Railway)"

5. **Настройте домен (опционально):**
   - Settings → Networking → Custom Domain
   - Добавьте ваш домен (например: `api.yourdomain.com`)

6. **Деплой:**
   - Railway автоматически задеплоит при каждом push в main
   - Или можно деплоить вручную

### Структура проекта на Railway:
```
Repository Root
└── backend/  ← Root Directory в Railway
    ├── app/
    ├── Dockerfile
    ├── requirements.txt
    └── ...
```

---

## 🔄 Миграция с текущего деплоя

### Пошаговый план:

1. **Подготовка:**
   - ✅ Создайте аккаунты на Vercel и Railway
   - ✅ Подготовьте все переменные окружения
   - ✅ Проверьте, что код работает локально

2. **Деплой Backend на Railway:**
   - Подключите репозиторий
   - Настройте переменные окружения
   - Проверьте, что API работает
   - Получите URL бэкенда (например: `https://your-backend.railway.app`)

3. **Деплой Frontend на Vercel:**
   - Подключите репозиторий
   - Укажите Root Directory: `system-analyst-assessment`
   - Установите `NEXT_PUBLIC_API_URL` на URL бэкенда с Railway
   - Настройте остальные переменные
   - Проверьте, что фронтенд работает

4. **Тестирование:**
   - Проверьте все функции приложения
   - Убедитесь, что CORS настроен правильно
   - Проверьте работу API запросов

5. **Настройка доменов (опционально):**
   - Добавьте кастомные домены
   - Обновите `CORS_ORIGINS` на бэкенде
   - Обновите `NEXT_PUBLIC_API_URL` на фронтенде

6. **Отключение старого деплоя:**
   - После успешного тестирования можно отключить старый сервер
   - Или оставить как резервный

---

## ⚠️ Важные замечания

### Безопасность:
1. **Никогда не коммитьте** переменные окружения в Git
2. Используйте **секреты** в Vercel и Railway
3. `API_KEY` должен быть достаточно сложным (минимум 32 символа)
4. `SECRET_KEY` должен быть уникальным и случайным

### CORS:
- Если фронтенд не может делать запросы к бэкенду, проверьте `CORS_ORIGINS`
- Убедитесь, что URL в `CORS_ORIGINS` точно совпадает с URL фронтенда (включая `https://`)
- Можно добавить несколько доменов через запятую

### MongoDB:
- Railway предоставляет MongoDB как сервис
- Или можно использовать MongoDB Atlas (бесплатный tier доступен)
- Не забудьте импортировать вопросы в MongoDB после создания базы

### Мониторинг:
- Vercel предоставляет логи и метрики
- Railway предоставляет логи и метрики
- Настройте алерты для критических ошибок

---

## 📝 Чеклист перед деплоем

### Frontend (Vercel):
- [ ] Репозиторий подключен к Vercel
- [ ] Root Directory установлен: `system-analyst-assessment`
- [ ] `NEXT_PUBLIC_API_URL` установлен на URL бэкенда
- [ ] `NEXT_PUBLIC_API_KEY` установлен и совпадает с бэкендом
- [ ] `NEXT_PUBLIC_ENABLE_QUICK_TEST` установлен
- [ ] Домен настроен (если нужен кастомный)
- [ ] Тестовый деплой успешен

### Backend (Railway):
- [ ] Репозиторий подключен к Railway
- [ ] Root Directory установлен: `backend`
- [ ] MongoDB настроен (Railway или внешний)
- [ ] Все переменные окружения установлены
- [ ] `CORS_ORIGINS` включает URL фронтенда
- [ ] `API_KEY` совпадает с `NEXT_PUBLIC_API_KEY` на фронтенде
- [ ] Домен настроен (если нужен кастомный)
- [ ] Health check endpoint работает: `/health`
- [ ] Тестовый деплой успешен

### Интеграция:
- [ ] Фронтенд может делать запросы к бэкенду
- [ ] CORS работает корректно
- [ ] API ключ работает
- [ ] Все функции приложения работают
- [ ] Логи не показывают ошибок

---

## 🆘 Troubleshooting

### Проблема: CORS ошибки
**Решение:** Проверьте, что `CORS_ORIGINS` на бэкенде включает точный URL фронтенда (с `https://`)

### Проблема: 401 Unauthorized
**Решение:** Убедитесь, что `NEXT_PUBLIC_API_KEY` на фронтенде совпадает с `API_KEY` на бэкенде

### Проблема: Backend не отвечает
**Решение:** 
- Проверьте логи на Railway
- Убедитесь, что все переменные окружения установлены
- Проверьте health check: `https://your-backend.railway.app/health`

### Проблема: Frontend не собирается
**Решение:**
- Проверьте логи сборки на Vercel
- Убедитесь, что все `NEXT_PUBLIC_*` переменные установлены
- Проверьте, что нет синтаксических ошибок

---

## 📚 Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Railway Documentation](https://docs.railway.app)
- [Next.js Environment Variables](https://nextjs.org/docs/basic-features/environment-variables)
- [FastAPI CORS](https://fastapi.tiangolo.com/tutorial/cors/)

---

**Готово к миграции!** 🚀

